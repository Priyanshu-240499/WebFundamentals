// settimeout and setInterval are used if you want to make changes when your webpage loades after certain time
// we can use them.They are not JS functions but browser function

// set time out only once after some time while setInterval continouslly works after certain time again and again

// setTimeout(function() {   //-----syntax------
    
// }, 3000);

// setIntervals(function() {   //-----syntax------
    
    // }, 3000);

